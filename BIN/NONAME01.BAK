#include<stdio.h>
#include<conio.h>
void main()
{
int n,i;
float a[100],sum=0.0;
printf("Enter size:");
scanf("%d",&n);
printf("Enter array elements");
for(i=0;i<n;i++)
{
scanf("%d",&a[i]);
}
for(i=0;i<n;i++)
{
sum=sum+a[i];
}
printf("Sum of elements");
getch();
}